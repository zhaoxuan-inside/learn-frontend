# HTML 选择器，为本元素选择指定的 CSS 样式

> [!cite]+ ID 选择器
> ```css
> #bg_blue {
>   backgroup-color: blue;    
> }
> ```

> [!cite]+ 类选择器
> ```css
> .bg_red {
>     backgroup-color: red;
> }
> ```

> [!cite]+ 伪类选择器
> ```css
> [ele_type]:hover {
>     background-color: green;
> }
